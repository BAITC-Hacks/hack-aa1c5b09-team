spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://127.0.0.1:55432/needboard_test}
    username: ${TEST_DB_USERNAME:needboard}
    password: ${TEST_DB_PASSWORD:}
logging:
  level:
    root: WARN
